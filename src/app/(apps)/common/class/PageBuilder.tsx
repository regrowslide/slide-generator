'use client'

import {roleMaster} from '@cm/class/builders/PageBuilderVariables'

export class PageBuilder {
  static store = undefined // デフォルトのMyFormを使用

  static user = undefined // デフォルトのMyFormを使用

  static roleMaster = roleMaster // 権限割当表を表示
}
