/**
 * WorkCard関連の定数定義
 */

/** プレースホルダー表示の遅延時間（ミリ秒） */
export const PLACEHOLDER_DELAY_MS = 300

/** Intersection Observerの設定 */
export const WORK_CARD_INTERSECTION_CONFIG = {
  TRIGGER_ONCE: true,
  THRESHOLD: 0.1,
} as const

/** モバイル判定のブレークポイント（ピクセル） */
export const MOBILE_BREAKPOINT = 640

